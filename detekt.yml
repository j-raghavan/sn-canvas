build:
  maxIssues: 0

style:
  MagicNumber:
    active: false # canvas math (SuperCanvasCore.kt) is inherently numeric; named constants add noise, not clarity
  MaxLineLength:
    maxLineLength: 140 # matches ktlintFormat's own single-line-expression-body collapsing output
